"""
Local LLM expertise modules. Each module has its own training-data layout under input/<module>/<domain>/.
"""
# Questionnaire: summary from filled questionnaire (agreed + background)
# Other modules can be added (e.g. another_module) with input/<module>/<domain>/
